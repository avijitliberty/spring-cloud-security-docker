spring:
  profiles:
    active:
    - test
  application:
    name: user-server
  jpa:
    database: HSQL
    show-sql: true

# Make the application available at http://localhost:8082
server:
  port: 8082
  contextPath: /services