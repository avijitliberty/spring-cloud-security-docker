spring:
  profiles: test
  application:
    name: note-server
  jpa:
    database: HSQL
    show-sql: true

# Make the application available at http://localhost:8082
server:
  port: 8084
  contextPath: /services
  
eureka:
  client:
    register-with-eureka: false
    fetchRegistry: false